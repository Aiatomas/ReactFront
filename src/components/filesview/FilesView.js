import React, {useEffect, useState} from "react";
import MainBtn from "../btn/MainBtn";
import {useSelector} from "react-redux";

const FilesView = () => {
    // const files = useSelector((state) => state.allFiles.allFiles);
    // const [allFiles, setAllFiles] = useState((state) => state.allFiles);
    // const files = useEffect((state) => state.allFiles);

    return (
        <div>
            {/*{allFiles.map((item, id, name) => (*/}
            {/*    <MainBtn key={id} text={name}></MainBtn>*/}
            {/*))}*/}
        </div>
    );
};

export default FilesView;